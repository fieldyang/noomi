{
    "namespace":"/",        //命名空间
    "files":[],             //导入文件数组，相对于该该文件路径
    "routes":[              // 路由配置
        {
            "path":"userq_*",       //路径，带*表示可以被替换，如xxx/yyy*，匹配该类中的所有方法
            "class":"UserQuery",    //类
            "method":""             //方法，可以是具体的方法名，也可以是空，表示通配符
        }
    ]
}


{
	"app_name":"App", 	//应用名，当一个服务器运行多个应用时，需要配置
	"language":"zh", 	//框架提示语言(可选配置)，zh中文，en英文，默认zh
	"open_watcher":true, //打开file watcher
	"cluster":false,		//集群设置，如果设置为true，则表示运行于集群环境，所有NCaceh的save_type都设置为1
	"web":"web.json",
	"instance":"instance.json",
	// "launchhook":"launchhook.json",
	
	// "aop":"aop.json",
	"database":"database/relaen.json",
	// "route":"route.json",
	// "filter":"filter.json",
	"redis":"redis.json"
	// "security":"security.json"
	
}
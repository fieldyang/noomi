{
    "filters":[
        {
            "instance_name":"pathFilter",   //实例名，必填
            "url_pattern":"/*",             //过滤url，支持通配符*，默认 "/*"
            "method_name":"do2"             //实例中的方法名，默认do
        } 
    ]
}